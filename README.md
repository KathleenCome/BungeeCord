This is the fork of [BungeeCord](https://github.com/SpigotMC/BungeeCord)
------------------------------------------
The fork add a setting to let user change the session url.
In the `config.yml`, you can change the session url.